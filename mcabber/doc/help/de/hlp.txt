
 /HELP [command|+topic]

Zeigt die Hilfe zu einem Befehl oder ein Thema an.
Wenn kein Argument gegeben ist, wird diese Hilfe angezeigt.
Verfügbare Befehle: add, alias, authorization, bind, buffer, clear, connect, del, disconnect, event, group, help, info, move, msay, pgp, quit, rawxml, rename, request, room, roster, say_to, say, set, status_to, status, version.
